import { ClickButtonDirective } from './click-button.directive';

describe('ClickButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickButtonDirective();
    expect(directive).toBeTruthy();
  });
});
